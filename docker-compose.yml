version: "3"

services:
  pg-tasks:
    container_name: pg-tasks
    image: postgres:9.6
    ports: 
      - "5433:5432"
    environment:
      - POSTGRES_DB=tasks
      # - POSTGRES_USER=tasks_user
      - POSTGRES_PASSWORD=password